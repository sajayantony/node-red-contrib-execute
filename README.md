# Execute Node with combined outputs
The default node-red exec has 3 outputs. This doesn't not compose well with http messages. This module is based of the origin exe  and merely combines them together.
